name: aws-pod-identity-webhook
namespace: kube-system
serviceType: managed

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

image:
  registry: quay.io
  name: giantswarm/aws-pod-identity-webhook
  tag: v0.0.1
  pullPolicy: IfNotPresent
